[Unit]
Description=gunicorn daemon
Requires=gunicorn.socket
After=network.target

[Service]
User=root
Group=root
WorkingDirectory={{ aws.instance.project.django.dir.path }}
ExecStart={{ aws.instance.project.venv.dir.path }}/bin/gunicorn --config {{ aws.instance.gunicorn.config.dir.path }}/gunicorn.conf.py {{ aws.instance.project.django.name }}.wsgi

[Install]
WantedBy=multi-user.target
